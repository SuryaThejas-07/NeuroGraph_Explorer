# NeuroGraph_Explorer
The project demonstrates a Graph Convolutional Network (GCN) applied to the Karate Club dataset. It involves loading and preparing graph data, defining and training a GCN model for node classification (assigning nodes to 'countries'), and visualizing the training process and learned node embeddings in both 2D graph animations and 3D scatter plots. 
